<div class="bookmarks list h-feed">
  <%= for bookmark <- @bookmarks do %>
    <%= render "bookmark.html", conn: @conn, bookmark: bookmark %>
  <% end %>
</div>

<%= if @logged_in do %>
  <%= link "New Bookmark", to: Routes.bookmark_path(@conn, :new), class: "btn btn-outline-primary" %>
<% end %>
