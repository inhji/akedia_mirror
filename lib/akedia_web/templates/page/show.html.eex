<h1><%= @page.title %></h1>

<p>
  <%= raw(AkediaWeb.Markdown.to_html(@page)) %>
</p>

<%= if @current_user do %>
  <%= link "Edit", to: Routes.page_path(@conn, :edit, @page), class: "btn btn-outline-primary" %>
<% end %>
