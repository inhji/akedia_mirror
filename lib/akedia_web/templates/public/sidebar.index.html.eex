<%= if @logged_in do %>
<div class="card">
	<div class="card-content">		
    <aside class="menu">
      <p class="menu-label">
        User
      </p>
      <ul class="menu-list">
        <li>
        	<%= link to: Routes.user_path(@conn, :show) do %>
						<i class="fa fa-user"></i>
        		<span>Profile</span>
        	<% end %>
        </li>
      </ul>
      <p class="menu-label">
        Content
      </p>
       <ul class="menu-list">
        <li>
        	<%= link to: Routes.bookmark_path(@conn, :new) do %>
        		<i class="fa fa-bookmark-o"></i>
        		<span>New Bookmark</span>
        	<% end %>
        </li>
        <li>
        	<%= link to: Routes.post_path(@conn, :new) do %>
        		<i class="fa fa-sticky-note-o"></i>
        		<span>New Post</span>
        	<% end %>
        </li>
      </ul>
    </aside>
	</div>
</div>
<% end %>

<%= if @weather do %>
<div class="card weather">
	<div class="card-content">	
		<div class="media">
			<figure class="media-left">
				<p class="image is-64x64">
					<canvas id="weather" width="48" height="48" data-icon="<%= @weather.icon %>" class="<%= @weather.icon %>"></canvas>
				</p>
			</figure>
			<div class="media-content">
				<div class="content">
					<strong><%= @weather.now %> at <%= @weather.temperature %> °C</strong>
					<small>(Lo: <%= @weather.min %> / Hi: <%= @weather.max %>)</small>
					<p>
						Looks like it's going to be <%= String.downcase(@weather.summary) %>
					</p>
				</div>
			</div>
		</div>
	</div>
</div>
<% end %>

<div class="card listens">
  <div class="card-content">
  	<%= render SharedView, "listens/listen.html", conn: @conn, listen: @last_listen %>
  </div>
</div>