<section class="profile">
   <%= render SharedView, "hcard.html", assigns %>
</section>

<%= if @logged_in do %>
	<%= live_render(@conn, AkediaWeb.PostFormLive) %>
<% end %>

<div class="columns is-desktop">
	<div class="column">
		<%= render "entity_feed.html", assigns %>

		<%= if @total_pages != 1 do %>
			<%= pagination_links @page,
				previous: ~E(<span>←</span>),
				next: ~E(<span>→</span>)
			%>
		<% end %>
	</div>
</div>

