<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

    <%= live_title_tag assigns[:page_title] || "Home", suffix: " - Akedia" %>

    <%= if @has_user do %>
      <%= for profile <- @current_user.profiles do %>
        <link rel="<%= profile.rel_value %>" href="<%= profile.url %>" />
      <% end %>
    <% end %>

    <link rel="apple-touch-icon" sizes="180x180" href="<%= Routes.static_path(@conn, "/images/favicon/apple-touch-icon.png") %>">
    <link rel="icon" type="image/png" sizes="32x32" href="<%= Routes.static_path(@conn, "/images/favicon/favicon-32x32.png") %>">
    <link rel="icon" type="image/png" sizes="16x16" href="<%= Routes.static_path(@conn, "/images/favicon/favicon-16x16.png") %>">
    <link rel="mask-icon" href="<%= Routes.static_path(@conn, "/images/favicon/safari-pinned-tab.svg") %>" color="#4c00ff">
    <link rel="manifest" href="<%= Routes.static_path(@conn, "/site.webmanifest") %>">
    <meta name="msapplication-TileColor" content="#4c00ff">
    <meta name="theme-color" content="#4c00ff">

    <%= csrf_meta_tag() %>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>

  <body class="has-navbar-fixed-top" data-js-view-path="<%= js_view_path(@conn) %>"> 
    <header>
      <nav role="navigation" class="main-nav" id="main-nav">
        <%= render "navigation.html", assigns %>
      </nav>
    </header>

    <%= @inner_content %>

    <footer class="footer" id="footer">
      <%= render "footer.html", assigns %>
    </footer>
  </body>
</html>
